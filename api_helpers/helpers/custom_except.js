# export function handleException(exType, exInfo, tb) {
#     let myPaths = getMyPaths()
#     let sSum = traceback.extractTb(tb)
#     for s in sSum:
#         if myPaths.includes(s.filename):
#             let f = os.path.relpath(s.filename)
#             print("File ", f)
#             print("    Line {}, {}".format(s.lineno, s.Line))
#             print("    Error:{}".format(exInfo))
# sys.let excepthook = handleException
if (_Name__ === "_Main__") {
    let a = [1, 2, 3]
}
    let b = a[3]

